<table border="1" class="comments_table">
	<% @comments.each do |comment| %>
	<tr>
		<td><%= comment.description %></td>
	</tr>
	<tr>
		<td style="text-align: right">-- <%= User.find(comment.user_id).full_name%></td>
	</tr>
	<% end %>
</table>