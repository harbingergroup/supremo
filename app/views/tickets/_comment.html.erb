 <table>
  <% @comments.each do |comment| %>
  <tr>
    <td><%= comment.description %></td>
    <td><%= link_to "Edit",edit_ticket_comment_path(:ticket_id => @ticket.id, :id => comment.id), :remote => true %></td>
    <td><%= link_to "Destroy", ticket_comment_path(:ticket_id => @ticket.id, :id => comment.id), :remote => true, :confirm => 'Are you sure?', :method => :delete %></td>

  </tr>
<% end %>
  </table>